__pycache__/
*.pyc
.DS_Store
node_modules/
package-lock.json

# Screenshots written by test runs
.test-shots/

# Individual model previews are regenerable (npm run assets:preview);
# only the combined contact sheet is committed.
docs/previews/*.png
!docs/previews/contact_sheet.png

# Android build outputs and signing material. The keystore and its passwords
# must never be committed: losing control of them means losing the app.
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/
android/local.properties
android/keystore.properties
*.keystore
*.jks
